[Unit]
Description=Disable NVIDIA card on resume
After=suspend.target sleep.target

[Service]
Type=oneshot
ExecStart=/bin/sh -c 'gpu-ctl s && gpu-ctl f && gpu-ctl s'

[Install]
WantedBy=multi-user.target